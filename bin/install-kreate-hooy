#!/usr/bin/env bash

version=$1

dir="${2:-opt}/karmah/${version}"
file="karmah-${version}.tar.gz"
url="https://github.com/kisst-org/karmah/releases/download/${version}/${file}"

mkdir -p "${dir}"
curl -L  "${url}" | tar xvz -C "${dir}"
cd bin
ln -sf "../${dir}"/karmah .
