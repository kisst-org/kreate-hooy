
render_copy-files() {
    karmah_file_dir=$(dirname "${karmah_file}")
    files_list="$karmah_file_dir"/${env}/files/*.yaml
    verbose_cmd cp -f ${files_list} ${output_dir}
}

apply_update_copy-files() { :; }
